/**
 * Personal Finance — обгортка над income book + cash flows для individual.
 * Поки що re-export точок входу; повна композиція — у наступних фазах.
 */
export { useIncomeBookStatus } from "@/hooks/useIncomeBookStatus";
