import type { ProcessTemplate } from "../types";

/** Adapter: auto_sign_rules → ProcessTemplate (kind="auto_sign"). */
export function fromAutoSignRules(cabinetId: string): ProcessTemplate[] {
  return [];
}
