import type { ProcessTemplate } from "../types";

/** Adapter: useProactiveNudges → ProcessTemplate (kind="nudge"). */
export function fromProactiveNudges(cabinetId: string): ProcessTemplate[] {
  return [];
}
